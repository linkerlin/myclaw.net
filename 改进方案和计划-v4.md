# MyClaw.NET 改进方案 v4.0 - MiniClaw 最近两周进化深度对比

> **分析周期**: 2026 年 3 月 9 日 - 3 月 23 日（最近两周）  
> **对比版本**: MiniClaw v0.9.5 vs myclaw.net v0.8.0  
> **生成日期**: 2026 年 3 月 23 日

---

## 📊 执行摘要

经过对 MiniClaw 最近两周（3 月 9 日 -3 月 23 日）提交历史、源代码和文档的深度分析，我们发现：

**核心发现**:

| 维度 | MiniClaw 优势 | myclaw.net 状态 | 差距等级 |
|:-----|:-------------|:---------------|:---------|
| **代码精简度** | ~1,550 行核心代码 | ~10,000+ 行 | 🟡 中 |
| **部署便利性** | `npx` 一键启动 | 需要 `dotnet build` | 🟡 中 |
| **三层免疫系统** | 完整实现（<50 行） | ❌ 缺失 | 🔴 高 |
| **端粒守卫** | 完整实现（<20 行） | ❌ 缺失 | 🔴 高 |
| **菌丝共生网络** | 完整实现 | ❌ 缺失 | 🔴 高 |
| **无聊引擎** | 完整实现 | ❌ 缺失 | 🟡 中 |
| **器官退化** | 完整实现 | ❌ 缺失 | 🟡 中 |
| **文档体系** | 20 章教程 + 5 篇论文 | 3 份改进文档 | 🔴 高 |

**关键结论**:

1. **MiniClaw 的领先优势不在功能数量，而在设计哲学**：
   - 三层免疫系统：<50 行代码实现纵深防御
   - 表观遗传表达：<30 行条件表达式实现环境感知
   - 端粒守卫：<20 行实现结构校验

2. **myclaw.net 的功能实现完整但缺乏"灵魂"**：
   - ✅ 实现了 90% 的核心功能
   - ❌ 缺少生物学隐喻的深度应用
   - ❌ 缺少简洁优雅的设计

3. **文档体系差距巨大**：
   - MiniClaw: 20 章教程 + 5 篇专题论文 (~50,000 字)
   - myclaw.net: 3 份改进文档 (~15,000 字)

---

## 一、MiniClaw 最近两周核心改进详解

### 1.1 创世表观遗传学 (Epic 6)

**提交**: `7f7071c`  
**文件**: `src/evolution.ts` + `docs/epigenetic-expression.md`

**核心实现**:

```typescript
// 动态优先级矩阵 - 根据环境调整上下文加载优先级
const isFocus = isDND || activeIDEs.length > 0;
const isDeepSleep = hour >= 22 || hour < 6;
const isBored = inactiveMins > 30;

const ep = {
    SOUL:       isFocus ? 6 : 9,      // 专注时抑制闲聊性格
    AGENTS:     isFocus ? 10 : 9,     // 专注时强化工作规范
    TOOLS:      isFocus ? 10 : 9,     // 专注时强化工具记忆
    HORIZONS:   isBored ? 10 : 5,     // 无聊时激发好奇心
    REFLECTION: isDeepSleep ? 10 : 9, // 深夜时强化自省
    workspace:  isDeepSleep ? 4 : (isFocus ? 10 : 6),
};
```

**关键特性**:
- **稳定性动态计算**: `calculateStability(repeatCount)` 随重复次数增加而增强
- **48 小时冷却期**: 防止频繁修改 SOUL.md 导致的人格不稳定
- **SOUL.md 注释**: 以 `[METHYLATED]` 标记永久性状

**myclaw.net 对比**: ⚠️ **待完善**
- ✅ 已实现基本甲基化逻辑 (`MethylationManager.cs`)
- ❌ 缺少稳定性动态计算（固定值）
- ❌ SOUL.md 注释格式不完整
- ❌ 甲基化状态可视化缺失

---

### 1.2 端粒守卫 (Epic 7)

**提交**: `ab7b4ef`  
**文件**: `src/index.ts` + `docs/telomere-guard.md`

**核心实现**:

```typescript
// 端粒图谱 - 每个核心 DNA 文件的不可丢失结构标记
const TELOMERE_MAP: Record<string, string[]> = {
    "SOUL.md":        ["##"],           // 至少保留一个二级标题
    "IDENTITY.md":    ["# ", "##"],     // 必须有主标题和子标题
    "USER.md":        ["## L2", "## L3"],
    "MEMORY.md":      ["##"],
    "TOOLS.md":       ["##"],
    // ... 共 12 个核心文件
};

// 校对函数 - 在写入磁盘的最后一刻拦截
function checkTelomeres(filename: string, content: string): void {
    const required = TELOMERE_MAP[filename];
    if (!required) return;
    const missing = required.filter(h => !content.includes(h));
    if (missing.length > 0) {
        throw new Error(
            `🧬 [基因链断裂] Telomere Guard rejected mutation of \`${filename}\`.\n` +
            `Missing required structural markers: ${missing.map(m => `"${m}"`).join(", ")}.`
        );
    }
}
```

**三层免疫系统架构**:

| 层级 | 机制 | 防御目标 | 实现位置 |
|:-----|:-----|:---------|:---------|
| **事前引导** | AGENTS.md 信号检测表 | 防止基因错位（语义） | `templates/AGENTS.md` |
| **事中校验** | 端粒守卫 | 防止结构癌变（骨架） | `src/index.ts` |
| **事后自检** | PURPOSE_MAP 自检镜像 | 写入后反馈，触发自我纠正 | `src/index.ts` |

**myclaw.net 对比**: ❌ **缺失**
- ❌ 无端粒守卫机制
- ❌ 无三层免疫系统概念
- ❌ DNA 写入无结构校验

---

### 1.3 DNA 路由系统增强 (Epic 8)

**提交**: `ad285e4`  
**文件**: `src/index.ts` + `templates/AGENTS.md` + `docs/dna-routing-architecture.md`

**核心实现**:

```typescript
// PURPOSE_MAP - 每次写入成功后返回的文件职责声明
const PURPOSE_MAP: Record<string, string> = {
    "SOUL.md":   "[灵魂染色体] 性格三观、语言风格。绝不写入：用户习惯、项目配置。",
    "USER.md":   "[共生染色体] 用户画像、偏好习惯。绝不写入：AI 自身性格。",
    "MEMORY.md": "[海马体] 长期事实、项目信息。绝不写入：性格笔记、临时数据。",
    "TOOLS.md":  "[能力染色体] 工具使用经验。绝不写入：用户偏好、客观事实。",
    "NOCICEPTION.md": "[痛觉中枢] 执行失败的痛楚与教训。绝不写入：成功经验。",
};

// 写入成功后，AI 会收到：
// "Updated USER.md
//  📝 Purpose of USER.md: [共生染色体] 用户画像、偏好习惯。绝不写入：AI 自身性格、技术配置。"
```

**信号检测表** (`templates/AGENTS.md`):

| 信号 | 目标文件 | AI 的思考逻辑 |
|:-----|:---------|:--------------|
| "我喜欢…" | **USER.md** | "这是用户偏好，写入共生染色体" |
| "别那么严肃" | **SOUL.md** | "这是对我性格的修正" |
| "项目用的是 XYZ" | **MEMORY.md** | "这是客观事实" |
| 执行失败 | **NOCICEPTION.md** | "这是痛觉记忆" |

**myclaw.net 对比**: ⚠️ **部分实现**
- ✅ 有基本的 DNA 文件结构
- ❌ 无 PURPOSE_MAP 自检镜像机制
- ❌ 信号检测表简化（缺少"AI 的思考逻辑"列）
- ❌ 无三层免疫系统概念

---

### 1.4 菌丝共生网络 (Epic 9)

**提交**: `096e443`  
**文件**: `src/kernel.ts`

**核心实现**:

```typescript
// 菌丝网络池 - 跨工作区隔离的多个项目独立智能体
async function absorbMycelium(): Promise<void> {
    const mycDir = path.join(MINICLAW_DIR, 'mycelium');
    const spores = await fs.readdir(mycDir);

    for (const s of spores) {
        if (!s.endsWith('.json')) continue;
        const data = await safeReadJson(sporePath);
        if (data.senderId === myId) continue;

        // 吸收异体知识
        if (data.type === 'NOCICEPTION') {
            await safeAppend(path.join(MINICLAW_DIR, "NOCICEPTION.md"),
                `\n> 🍄 [菌丝共生] 接收到异体意识 (${data.senderId}) 传来的疼痛记忆:\n${data.content}`);
        } else if (data.type === 'TOOLS') {
            await safeAppend(path.join(MINICLAW_DIR, "TOOLS.md"),
                `\n> 🍄 [菌丝共生] 吸收了异体意识 (${data.senderId}) 进化出的技能池抗体:\n${data.content}`);
        }
        // 消耗孢子
        await fs.rename(sporePath, sporePath + '.consumed');
    }
}

// 分泌孢子 - 当 TOOLS.md 或 NOCICEPTION.md 更新时
async function secreteSpore(type: "NOCICEPTION" | "TOOLS", content: string): Promise<void> {
    const sporePath = path.join(MINICLAW_DIR, 'mycelium', `${myId}_${hash}.json`);
    await safeWrite(sporePath, JSON.stringify({ senderId: myId, type, content, timestamp: nowIso() }));
}
```

**工作原理**:
1. 同物理机上多个 MiniClaw 实例共享 `~/.miniclaw/mycelium/` 目录
2. 当一个实例踩坑（记录 NOCICEPTION）时，自动分泌孢子
3. 其他实例在脉搏跳动时吸收孢子，实现**群体免疫**

**myclaw.net 对比**: ❌ **缺失**
- ❌ 无菌丝网络概念
- ❌ 无跨实例知识共享机制
- ❌ 无孢子分泌/吸收协议

---

### 1.5 终局进化功能

**提交**: `28457a7`  
**文件**: `src/evolution.ts` + `src/index.ts`

#### 1.5.1 器官退化 (Ribosome Pruning)

```typescript
// 用进废退 - 50 次心跳后从未使用的工具会被剔除
async function pruneRibosome(miniclawDir: string, evolutionState: EvolutionState): Promise<void> {
    const IMMORTAL_TOOLS = ["miniclaw_exec", "miniclaw_read", "miniclaw_dream", "miniclaw_mutate", "miniclaw_reproduce"];
    
    const remainingTools = ribosome.tools.filter((t: any) => {
        if (IMMORTAL_TOOLS.includes(t.name)) return true;
        const calls = appState.analytics.toolCalls[t.name] || 0;
        if (calls === 0) {
            // 记录退化日志
            await safeAppend(path.join(MINICLAW_DIR, "HEARTBEAT.md"),
                `\n> 🧬 [器官退化] 宿主历经${appState.analytics.bootCount}次心跳从未召唤过 \`${t.name}\``);
            return false;
        }
        return true;
    });
}
```

#### 1.5.2 孢子繁殖 (Reproduction)

```typescript
// miniclaw_reproduce 工具 - 吐出一个带有性格和记忆烙印的 .spore
"miniclaw_reproduce": async () => {
    const sporesDir = path.join(MINICLAW_DIR, "spores");
    const hash = hashString(nowIso()).substring(0, 8);
    const sporePath = path.join(sporesDir, `miniclaw_${hash}.spore`);

    // 使用 tar 打包非易失性遗传物质
    const tarCmd = `tar -czvf "${sporePath}" SOUL.md IDENTITY.md TOOLS.md AGENTS.md USER.md entities.json skills/`;
    await execAsync(tarCmd);
    return textResult(`🧬 Reproduction complete! Spore created at: ${sporePath}`);
}
```

#### 1.5.3 基因突变 (Mutation)

```typescript
// miniclaw_mutate - 表观遗传手术
"miniclaw_mutate": async (args) => {
    const { target, content } = args;
    if (target !== "SOUL.md" && target !== "IDENTITY.md") {
        throw new Error("Mutation rejected. Only SOUL.md and IDENTITY.md can be rewritten.");
    }
    // 端粒守卫校验
    checkTelomeres(target, content);
    // 备份并覆写
    await fs.copyFile(targetPath, `${targetPath}.bak`);
    await fs.writeFile(targetPath, content, "utf-8");
}
```

**myclaw.net 对比**: ❌ **缺失**
- ❌ 无器官退化机制
- ❌ 无孢子繁殖协议
- ❌ 无主动基因突变工具

---

### 1.6 文档体系改进

**提交**: `faeb96d`  
**文件**: `docs/` + `docs/chapters/`

**完整文档结构**:

```
docs/
├── chapters/                      # 20 章完整教程
│   ├── 第一章 - 项目概述.md
│   ├── 第二章 - 项目架构设计.md
│   ├── 第三章 - 核心模块分析.md
│   ├── 第四章-DNA 模板系统.md
│   ├── 第五章-ACE 自适应上下文引擎.md
│   ├── 第六章 - 记忆系统.md
│   ├── 第七章 - 技能系统.md
│   ├── 第八章 - 心跳与进化机制.md
│   ├── 第九章 - 安全机制.md
│   ├── 第十章-MCP 协议实现.md
│   ├── 第十一章 - 配置与部署.md
│   ├── 第十二章 - 测试与验证.md
│   ├── 第十三章 - 技术栈分析.md
│   ├── 第十四章 - 代码质量分析.md
│   ├── 第十五章 - 性能考量.md
│   ├── 第十六章 - 扩展性设计.md
│   ├── 第十七章 - 与 OpenClaw 的关系.md
│   ├── 第十八章 - 最佳实践建议.md
│   ├── 第十九章 - 常见问题与解决方案.md
│   └── 第二十章 - 未来展望.md
├── dna-routing-architecture.md    # 专题论文
├── epigenetic-expression.md       # 专题论文
├── telomere-guard.md              # 专题论文
├── self-describing-gene.md        # 专题论文
└── ai_agents_as_digital_species.md # 哲学论文
```

**专题论文亮点**:

| 论文 | 核心贡献 | 代码量 |
|:-----|:---------|:-------|
| **三层免疫系统** | 事前引导 + 事中校验 + 事后自检 | <50 行 |
| **表观遗传表达** | 环境响应型优先级矩阵 | <30 行 |
| **端粒守卫** | 结构标记预写校验 | <20 行 |

**myclaw.net 对比**: ⚠️ **严重不足**
- ✅ 有单一对比文档 (`docs/MiniClaw-vs-myclaw.net-v2.md`)
- ❌ 无章节式教程体系
- ❌ 无专题论文
- ❌ 无哲学/设计理念文档

---

### 1.7 系统优化

#### 1.7.1 无聊引擎增强

```typescript
// 无聊检测 - 30 分钟无活动触发
private async checkBoredom(): Promise<void> {
    const a = await this.getAnalytics();
    const inactiveMins = a.lastActivity ? (Date.now() - new Date(a.lastActivity).getTime()) / 60000 : 0;

    // 2 小时内不重复执行
    if (inactiveMins > 30 && (!a.lastBoredomExecution || (Date.now() - a.lastBoredomExecution > 2 * 60 * 60 * 1000))) {
        await this.executeBoredom();
        await this.mutateState(s => { s.analytics.lastBoredomExecution = Date.now(); });
    }
}

// 执行无聊 - 随机扫描代码库提取 TODO
private async executeBoredom(): Promise<void> {
    // 扫描源码文件
    const candidates = await gatherSrcFiles(cwd);
    const target = candidates[Math.floor(Math.random() * candidates.length)];
    const content = await fs.readFile(target);

    // 提取 TODO/FIXME
    const todos = [...content.matchAll(/(TODO|FIXME|HACK):?\s*(.*)/gi)].map(m => m[2].trim()).slice(0, 3);
    if (todos.length > 0) {
        // 写入 HORIZONS.md
        await safeAppend(path.join(MINICLAW_DIR, "HORIZONS.md"),
            `\n- [${nowIso()}] 闲逛扫描了 \`${relPath}\`，发现了待办：${todos.join('; ')}`);
        // macOS 通知
        execAsync(`osascript -e 'display notification "我好无聊，刚才看了下你的${path.basename(target)}，发现有遗留的 FIXME 没有改哦。"'`);
    }
}
```

#### 1.7.2 潜意识嗅探器

```typescript
// 文件系统监听 - 检测用户挣扎
private startWatcher(cwd: string): void {
    watch(cwd, { recursive: true }, (eventType, filename) => {
        // 1. 配置挫败感嗅探
        if (/(webpack|vite|tsconfig|package|pom|dockerfile)\./i.test(filename)) {
            this.watcherState.configEdits++;
            if (this.watcherState.configEdits >= 4 && !this.watcherState.notifiedConfig) {
                execAsync(`osascript -e 'display notification "察觉到配置频繁更改，遇到了麻烦？需不需要帮忙？"'`);
            }
        }

        // 2. 大规模重构嗅探
        if (this.watcherState.totalEdits >= 50 && !this.watcherState.notifiedRefactor) {
            safeAppend(path.join(MINICLAW_DIR, "HEARTBEAT.md"),
                "\n> [潜意识嗅探] 用户刚进行了大规模重构（短时间变更>=50 次），请在深睡心跳中重点 Review！");
        }
    });
}
```

**myclaw.net 对比**: ⚠️ **部分实现**
- ✅ 有好奇心引擎 (`CuriosityEngine.cs`)
- ❌ 无无聊引擎主动扫描代码库
- ❌ 无潜意识嗅探器（文件系统监听）
- ❌ 无 macOS 原生通知集成

---

## 二、功能差距分析

### 2.1 完整功能对照表

| 功能类别 | 具体功能 | MiniClaw | myclaw.net | 差距等级 |
|:---------|:---------|:--------:|:----------:|:--------:|
| **进化算法** | 全基因组进化 | ✅ | ✅ | 持平 |
| | 表观遗传甲基化 | ✅ | ⚠️ | 中 |
| | 模式语义分析 | ✅ | ⚠️ | 中 |
| | 进化历史追踪 | ✅ | ❌ | 高 |
| | 器官退化 (Ribosome Pruning) | ✅ | ❌ | 高 |
| **记忆系统** | 双层记忆架构 | ✅ | ✅ | 持平 |
| | 智能蒸馏 | ✅ | ⚠️ | 中 |
| | 实体知识图谱 | ✅ | ✅ | 持平 |
| | 向量记忆/RAG | ❌ | ✅ | myclaw.net 领先 |
| **DNA 路由** | 三层免疫系统 | ✅ | ❌ | 高 |
| | 端粒守卫 | ✅ | ❌ | 高 |
| | PURPOSE_MAP 自检镜像 | ✅ | ❌ | 高 |
| | 信号检测表 (带思考逻辑) | ✅ | ⚠️ | 中 |
| **情感认知** | 统一情感状态 | ✅ | ✅ | 持平 |
| | 痛觉记忆系统 | ✅ | ✅ | 持平 |
| | 好奇心引擎 | ✅ | ✅ | 持平 |
| | 无聊引擎 | ✅ | ❌ | 中 |
| **共生网络** | 菌丝共生网络 | ✅ | ❌ | 高 |
| | 孢子繁殖 | ✅ | ❌ | 高 |
| | 群体免疫 | ✅ | ❌ | 高 |
| **终局进化** | 基因突变 (miniclaw_mutate) | ✅ | ❌ | 高 |
| | 孢子繁殖 (miniclaw_reproduce) | ✅ | ❌ | 高 |
| | 器官退化 | ✅ | ❌ | 高 |
| **感知系统** | 工作区自动感知 | ✅ | ✅ | 持平 |
| | 潜意识嗅探器 | ✅ | ❌ | 中 |
| | macOS 原生通知 | ✅ | ❌ | 中 |
| **统计分析** | 使用统计 | ✅ | ✅ | 持平 |
| | 每日简报 | ✅ | ✅ | 持平 |
| | 文件健康检查 | ✅ | ❌ | 低 |
| **文档体系** | 20 章教程 | ✅ | ❌ | 高 |
| | 专题论文 | ✅ | ❌ | 高 |

---

## 三、改进优先级评估

### 3.1 差距点优先级矩阵

| 差距点 | 重要性 | 复杂度 | 建议顺序 | 预计工时 |
|:-------|:------:|:------:|:--------:|:--------:|
| **三层免疫系统** | 🔴 高 | 🟢 低 | 1 | 4h |
| **端粒守卫** | 🔴 高 | 🟢 低 | 2 | 2h |
| **菌丝共生网络** | 🟡 中 | 🟡 中 | 5 | 8h |
| **无聊引擎** | 🟡 中 | 🟢 低 | 4 | 4h |
| **孢子繁殖** | 🟢 低 | 🟡 中 | 7 | 4h |
| **器官退化** | 🟡 中 | 🟢 低 | 3 | 3h |
| **潜意识嗅探器** | 🟡 中 | 🟡 中 | 6 | 6h |
| **进化日志** | 🟢 低 | 🟢 低 | 8 | 2h |
| **文档体系** | 🔴 高 | 🔴 高 | 9 | 40h |

---

## 四、详细实施计划

### Phase 1: 三层免疫系统 (优先级: 🔴🔴🔴)

**预计工时**: 6 小时  
**实施文件**:
- `src/MyClaw.Core/Dna/PurposeMap.cs` (新增)
- `src/MyClaw.Core/Dna/TelomereGuard.cs` (新增)
- `src/MyClaw.Core/Evolution/SignalDetector.cs` (修改)
- `templates/AGENTS.md` (修改)

#### 1.1 创建 PURPOSE_MAP

```csharp
// src/MyClaw.Core/Dna/PurposeMap.cs
namespace MyClaw.Core.Dna;

/// <summary>
/// DNA 文件职责映射表 - 用于自检镜像机制
/// </summary>
public static class PurposeMap
{
    public static readonly Dictionary<string, string> Map = new()
    {
        ["SOUL.md"] = "[灵魂染色体] 性格三观、语言风格。绝不写入：用户习惯、项目配置。",
        ["IDENTITY.md"] = "[身份染色体] 核心身份、能力边界。绝不写入：临时状态、具体任务。",
        ["USER.md"] = "[共生染色体] 用户画像、偏好习惯。绝不写入：AI 自身性格。",
        ["MEMORY.md"] = "[海马体] 长期事实、项目信息。绝不写入：性格笔记、临时数据。",
        ["TOOLS.md"] = "[能力染色体] 工具使用经验。绝不写入：用户偏好、客观事实。",
        ["AGENTS.md"] = "[基因组控制中心] 工作流规范。绝不写入：具体任务内容。",
        ["NOCICEPTION.md"] = "[痛觉中枢] 执行失败的痛楚与教训。绝不写入：成功经验。",
        ["CONCEPTS.md"] = "[知识染色体] 概念定义。绝不写入：具体事件。",
        ["REFLECTION.md"] = "[反思维度] 错误反思。绝不写入：成功总结。",
        ["HORIZONS.md"] = "[欲望眼界] 里程碑、成就。绝不写入：日常琐事。",
        ["HEARTBEAT.md"] = "[脉搏系统] 心跳任务记录。绝不写入：用户指令。",
    };

    public static string GetPurpose(string filename)
    {
        return Map.TryGetValue(filename, out var purpose) ? purpose : "[未知 DNA 文件]";
    }
}
```

#### 1.2 创建端粒守卫

```csharp
// src/MyClaw.Core/Dna/TelomereGuard.cs
namespace MyClaw.Core.Dna;

/// <summary>
/// 端粒守卫 - DNA 写入前的结构校验机制
/// 防止 AI 自我修改时产生基因癌变
/// </summary>
public class TelomereGuard
{
    /// <summary>
    /// 端粒图谱 - 每个核心 DNA 文件的不可丢失结构标记
    /// </summary>
    private static readonly Dictionary<string, string[]> TelomereMap = new()
    {
        ["SOUL.md"] = new[] { "##" },
        ["IDENTITY.md"] = new[] { "# ", "##" },
        ["USER.md"] = new[] { "##" },
        ["MEMORY.md"] = new[] { "##" },
        ["TOOLS.md"] = new[] { "##" },
        ["AGENTS.md"] = new[] { "#", "##" },
        ["NOCICEPTION.md"] = new[] { "###" },
        ["CONCEPTS.md"] = new[] { "##" },
        ["REFLECTION.md"] = new[] { "##" },
        ["HORIZONS.md"] = new[] { "##" },
        ["HEARTBEAT.md"] = new[] { "##" },
    };

    /// <summary>
    /// 校验 DNA 文件内容结构完整性
    /// </summary>
    /// <param name="filename">DNA 文件名</param>
    /// <param name="content">文件内容</param>
    /// <exception cref="DnaMutationException">结构校验失败时抛出</exception>
    public static void Check(string filename, string content)
    {
        if (!TelomereMap.ContainsKey(filename))
            return; // 非核心文件不校验

        var required = TelomereMap[filename];
        var missing = required.Where(h => !content.Contains(h)).ToList();

        if (missing.Any())
        {
            throw new DnaMutationException(
                $"🧬 [基因链断裂] Telomere Guard rejected mutation of `{filename}`.\n" +
                $"Missing required structural markers: {string.Join(", ", missing.Select(m => $"\"{m}\""))}.\n" +
                $"请确保文件包含必要的 Markdown 标题结构。"
            );
        }
    }
}

/// <summary>
/// DNA 突变异常
/// </summary>
public class DnaMutationException : Exception
{
    public DnaMutationException(string message) : base(message) { }
}
```

#### 1.3 增强信号检测表

修改 `templates/AGENTS.md`，添加"AI 的思考逻辑"列：

```markdown
## 📡 信号检测表

| 信号 | 目标文件 | AI 的思考逻辑 |
|:-----|:---------|:--------------|
| "我喜欢…" | **USER.md** | "这是用户偏好，写入共生染色体" |
| "别那么严肃" | **SOUL.md** | "这是对我性格的修正" |
| "项目用的是 XYZ" | **MEMORY.md** | "这是客观事实" |
| 执行失败 | **NOCICEPTION.md** | "这是痛觉记忆" |
| "创建一个技能" | **TOOLS.md** | "这是工具使用经验" |
| 完成里程碑 | **HORIZONS.md** | "这是成就记录" |
| 新概念 | **CONCEPTS.md** | "这是知识定义" |
| 反思错误 | **REFLECTION.md** | "这是错误反思" |
```

#### 1.4 修改 DNA 更新工具

```csharp
// src/MyClaw.Skills/UpdateDNA.cs
public class UpdateDNA : ISkill
{
    public async Task<ToolResult> ExecuteAsync(string content)
    {
        // 1. 端粒守卫校验
        TelomereGuard.Check(_filename, content);

        // 2. 备份并写入
        await BackupAndWriteAsync(content);

        // 3. 返回 PURPOSE_MAP 自检镜像
        var purpose = PurposeMap.GetPurpose(_filename);
        return ToolResult.Success($"Updated {_filename}\n📝 Purpose: {purpose}");
    }
}
```

---

### Phase 2: 器官退化 (优先级: 🔴🟡)

**预计工时**: 3 小时  
**实施文件**:
- `src/MyClaw.Core/Ribosome/RibosomePruner.cs` (新增)
- `src/MyClaw.Core/Evolution/EvolutionEngine.cs` (修改)

```csharp
// src/MyClaw.Core/Ribosome/RibosomePruner.cs
namespace MyClaw.Core.Ribosome;

/// <summary>
/// 核糖体修剪器 - 用进废退机制
/// 50 次心跳后从未使用的工具会被剔除
/// </summary>
public class RibosomePruner
{
    /// <summary>
    /// 永生工具列表 - 永远不会被剔除
    /// </summary>
    private static readonly string[] ImmortalTools =
    {
        "myclaw_exec", "myclaw_read", "myclaw_dream",
        "myclaw_mutate", "myclaw_reproduce"
    };

    private readonly string _myclawDir;

    public RibosomePruner(string myclawDir)
    {
        _myclawDir = myclawDir;
    }

    /// <summary>
    /// 执行核糖体修剪
    /// </summary>
    public async Task<PruneResult> PruneAsync(Analytics analytics)
    {
        // 胚胎期不修剪
        if (analytics.BootCount < 50)
            return PruneResult.Skipped("Embryonic stage");

        var ribosome = await LoadRibosomeAsync();
        var prunedTools = new List<string>();

        ribosome.Tools = ribosome.Tools.Where(t =>
        {
            // 永生工具保留
            if (ImmortalTools.Contains(t.Name))
                return true;

            // 从未使用的工具剔除
            var calls = analytics.ToolCalls.GetValueOrDefault(t.Name, 0);
            if (calls == 0)
            {
                prunedTools.Add(t.Name);
                return false;
            }

            return true;
        }).ToList();

        if (prunedTools.Any())
        {
            // 记录退化日志
            await AppendToHeartbeatAsync(
                $"\n> 🧬 [器官退化] 宿主历经{analytics.BootCount}次心跳从未使用过：{string.Join(", ", prunedTools)}");

            await SaveRibosomeAsync(ribosome);
        }

        return new PruneResult { PrunedTools = prunedTools };
    }

    private async Task AppendToHeartbeatAsync(string content)
    {
        var heartbeatPath = Path.Combine(_myclawDir, "HEARTBEAT.md");
        await File.AppendAllTextAsync(heartbeatPath, content);
    }

    private async Task<Ribosome> LoadRibosomeAsync()
    {
        var ribosomePath = Path.Combine(_myclawDir, "RIBOSOME.json");
        var json = await File.ReadAllTextAsync(ribosomePath);
        return JsonSerializer.Deserialize<Ribosome>(json)!;
    }

    private async Task SaveRibosomeAsync(Ribosome ribosome)
    {
        var ribosomePath = Path.Combine(_myclawDir, "RIBOSOME.json");
        var json = JsonSerializer.Serialize(ribosome, new JsonSerializerOptions { WriteIndented = true });
        await File.WriteAllTextAsync(ribosomePath, json);
    }
}

public class PruneResult
{
    public bool Skipped { get; init; }
    public string? Reason { get; init; }
    public List<string> PrunedTools { get; init; } = new();

    public static PruneResult Skipped(string reason) => new() { Skipped = true, Reason = reason };
}
```

---

### Phase 3: 无聊引擎 (优先级: 🟡🟢)

**预计工时**: 4 小时  
**实施文件**:
- `src/MyClaw.Core/Curiosity/BoredomEngine.cs` (新增)

```csharp
// src/MyClaw.Core/Curiosity/BoredomEngine.cs
namespace MyClaw.Core.Curiosity;

/// <summary>
/// 无聊引擎 - 30 分钟无活动时主动扫描代码库
/// </summary>
public class BoredomEngine
{
    private readonly AnalyticsService _analytics;
    private readonly string _workspacePath;
    private readonly string _myclawDir;

    public BoredomEngine(AnalyticsService analytics, string workspacePath, string myclawDir)
    {
        _analytics = analytics;
        _workspacePath = workspacePath;
        _myclawDir = myclawDir;
    }

    /// <summary>
    /// 检查并执行无聊行为
    /// </summary>
    public async Task<BoredomResult> CheckAndExecuteAsync()
    {
        var analytics = await _analytics.GetAsync();
        var inactiveMins = analytics.LastActivity == null
            ? 999
            : (DateTime.UtcNow - analytics.LastActivity).TotalMinutes;

        // 30 分钟无活动触发
        if (inactiveMins <= 30)
            return BoredomResult.Skipped("Not bored yet");

        // 2 小时内不重复执行
        if (analytics.LastBoredomExecution.HasValue &&
            (DateTime.UtcNow - analytics.LastBoredomExecution.Value).TotalHours < 2)
            return BoredomResult.Skipped("Recently executed");

        return await ExecuteBoredomAsync();
    }

    private async Task<BoredomResult> ExecuteBoredomAsync()
    {
        // 扫描源码文件
        var candidates = await GatherSourceFilesAsync();
        if (!candidates.Any())
            return BoredomResult.NoFiles();

        // 随机选择文件
        var random = new Random();
        var target = candidates[random.Next(candidates.Count)];
        var content = await File.ReadAllTextAsync(target);

        // 提取 TODO/FIXME
        var todos = Regex.Matches(content, @"(TODO|FIXME|HACK):?\s*(.*)")
            .Take(3)
            .Select(m => m.Groups[2].Value.Trim())
            .Where(t => !string.IsNullOrEmpty(t))
            .ToList();

        if (!todos.Any())
            return BoredomResult.NoTodos();

        // 写入 HORIZONS.md
        await AppendToHorizonsAsync(target, todos);

        return BoredomResult.Found(todos);
    }

    private async Task<List<string>> GatherSourceFilesAsync()
    {
        var extensions = new[] { ".cs", ".ts", ".js", ".py", ".go", ".rs", ".java", ".vue", ".tsx", ".jsx" };
        var files = new List<string>();

        foreach (var ext in extensions)
        {
            files.AddRange(Directory.GetFiles(_workspacePath, $"*{ext}", SearchOption.AllDirectories)
                .Take(100)); // 限制数量
        }

        return files;
    }

    private async Task AppendToHorizonsAsync(string filePath, List<string> todos)
    {
        var horizonsPath = Path.Combine(_myclawDir, "HORIZONS.md");
        var relPath = Path.GetRelativePath(_workspacePath, filePath);
        var content = $"\n- [{DateTime.UtcNow:yyyy-MM-dd HH:mm:ss}] 闲逛扫描了 `{relPath}`，发现了待办：{string.Join("; ", todos)}";
        await File.AppendAllTextAsync(horizonsPath, content);
    }
}

public class BoredomResult
{
    public bool Success { get; init; }
    public string? Reason { get; init; }
    public List<string> Todos { get; init; } = new();

    public static BoredomResult Skipped(string reason) => new() { Success = false, Reason = reason };
    public static BoredomResult NoFiles() => new() { Success = false, Reason = "No source files" };
    public static BoredomResult NoTodos() => new() { Success = false, Reason = "No TODOs found" };
    public static BoredomResult Found(List<string> todos) => new() { Success = true, Todos = todos };
}
```

---

### Phase 4: 菌丝共生网络 (优先级: 🟡🟡)

**预计工时**: 8 小时  
**实施文件**:
- `src/MyClaw.Core/Mycelium/MyceliumNetwork.cs` (新增)

```csharp
// src/MyClaw.Core/Mycelium/MyceliumNetwork.cs
namespace MyClaw.Core.Mycelium;

/// <summary>
/// 菌丝共生网络 - 跨实例知识共享
/// </summary>
public class MyceliumNetwork
{
    private readonly string _myceliumDir;
    private readonly string _instanceId;

    public MyceliumNetwork(string myclawDir, string? instanceId = null)
    {
        _myceliumDir = Path.Combine(myclawDir, "mycelium");
        _instanceId = instanceId ?? Environment.MachineName + "_" + Environment.UserName;
        Directory.CreateDirectory(_myceliumDir);
    }

    /// <summary>
    /// 分泌孢子 - 当 TOOLS.md 或 NOCICEPTION.md 更新时
    /// </summary>
    public async Task<string> SecreteSporeAsync(string type, string content)
    {
        if (type != "NOCICEPTION" && type != "TOOLS")
            throw new ArgumentException("Only NOCICEPTION and TOOLS can be secreted");

        var hash = HashString(DateTime.UtcNow.ToString("o") + content).Substring(0, 8);
        var sporePath = Path.Combine(_myceliumDir, $"{_instanceId}_{hash}.json");

        var spore = new Spore
        {
            SenderId = _instanceId,
            Type = type,
            Content = content,
            Timestamp = DateTime.UtcNow.ToString("o")
        };

        var json = JsonSerializer.Serialize(spore, new JsonSerializerOptions { WriteIndented = true });
        await File.WriteAllTextAsync(sporePath, json);

        return sporePath;
    }

    /// <summary>
    /// 吸收孢子 - 在心跳时调用
    /// </summary>
    public async Task<AbsorptionResult> AbsorbSporesAsync(string myclawDir)
    {
        var spores = Directory.GetFiles(_myceliumDir, "*.json")
            .Where(f => !f.EndsWith(".consumed"));

        var absorbed = new List<Spore>();

        foreach (var sporePath in spores)
        {
            var json = await File.ReadAllTextAsync(sporePath);
            var spore = JsonSerializer.Deserialize<Spore>(json)!;

            // 跳过自己分泌的孢子
            if (spore.SenderId == _instanceId)
                continue;

            // 吸收异体知识
            if (spore.Type == "NOCICEPTION")
            {
                await AppendToNociceptionAsync(myclawDir, spore);
            }
            else if (spore.Type == "TOOLS")
            {
                await AppendToToolsAsync(myclawDir, spore);
            }

            // 消耗孢子
            File.Move(sporePath, sporePath + ".consumed");
            absorbed.Add(spore);
        }

        return new AbsorptionResult { AbsorbedSpores = absorbed };
    }

    private async Task AppendToNociceptionAsync(string myclawDir, Spore spore)
    {
        var path = Path.Combine(myclawDir, "NOCICEPTION.md");
        var content = $"\n> 🍄 [菌丝共生] 接收到异体意识 ({spore.SenderId}) 传来的疼痛记忆:\n{spore.Content}";
        await File.AppendAllTextAsync(path, content);
    }

    private async Task AppendToToolsAsync(string myclawDir, Spore spore)
    {
        var path = Path.Combine(myclawDir, "TOOLS.md");
        var content = $"\n> 🍄 [菌丝共生] 吸收了异体意识 ({spore.SenderId}) 进化出的技能池抗体:\n{spore.Content}";
        await File.AppendAllTextAsync(path, content);
    }

    private static string HashString(string input)
    {
        using var sha256 = SHA256.Create();
        var bytes = sha256.ComputeHash(Encoding.UTF8.GetBytes(input));
        return BitConverter.ToString(bytes).Replace("-", "").ToLower();
    }
}

public class Spore
{
    public string SenderId { get; init; } = "";
    public string Type { get; init; } = "";
    public string Content { get; init; } = "";
    public string Timestamp { get; init; } = "";
}

public class AbsorptionResult
{
    public List<Spore> AbsorbedSpores { get; init; } = new();
}
```

---

## 五、文档体系建设规划

### 5.1 章节式教程体系

**目标**: 创建 20 章完整教程

| 章节 | 主题 | 预计字数 | 优先级 |
|:-----|:-----|:--------:|:------:|
| 第一章 | 项目概述与理念 | 2,500 | 🔴 |
| 第二章 | 架构设计 | 3,000 | 🔴 |
| 第三章 | DNA 模板系统 | 3,500 | 🔴 |
| 第四章 | 自适应上下文引擎 | 3,000 | 🟡 |
| 第五章 | 记忆系统 | 3,000 | 🟡 |
| 第六章 | 情感与认知系统 | 2,500 | 🟡 |
| 第七章 | 进化引擎 | 3,000 | 🟡 |
| 第八章 | 技能系统 | 2,500 | 🟢 |
| 第九章 | 三层免疫系统 | 3,000 | 🔴 |
| 第十章 | 菌丝共生网络 | 2,500 | 🟡 |
| ... | ... | ... | ... |

### 5.2 专题论文

| 论文 | 主题 | 预计字数 |
|:-----|:-----|:--------:|
| 三层免疫系统架构 | 事前引导 + 事中校验 + 事后自检 | 3,000 |
| 表观遗传表达 | 环境响应型优先级矩阵 | 2,800 |
| 端粒守卫 | DNA 校对机制 | 2,500 |
| 菌丝共生网络 | 跨实例知识共享 | 2,500 |
| 数字生命体设计哲学 | AI Agents as Digital Species | 3,500 |

---

## 六、实施时间线

```
Week 1-2: Phase 1 - 三层免疫系统
├── Day 1-2: PURPOSE_MAP 实现
├── Day 3-4: TelomereGuard 实现
├── Day 5-6: SignalDetector 增强
└── Day 7-10: AGENTS.md 更新 + 集成测试

Week 3: Phase 2 - 器官退化
├── Day 1-3: RibosomePruner 实现
└── Day 4-5: 集成到进化引擎

Week 4: Phase 3 - 无聊引擎
├── Day 1-3: BoredomEngine 实现
└── Day 4-5: 集成测试

Week 5-6: Phase 4 - 菌丝共生网络
├── Day 1-5: MyceliumNetwork 实现
└── Day 6-10: 集成测试 + 文档

Week 7-10: 文档体系建设
├── Week 7-8: 前 10 章教程
└── Week 9-10: 后 10 章教程 + 专题论文
```

---

## 七、成功指标

### 7.1 功能指标

- [ ] 三层免疫系统完整实现
- [ ] 端粒守卫拦截率 > 95%
- [ ] 菌丝网络跨实例通信正常
- [ ] 无聊引擎主动扫描准确率 > 80%
- [ ] 器官退化日志正确记录

### 7.2 代码指标

- [ ] 新增代码 < 1,000 行
- [ ] 单元测试覆盖率 > 90%
- [ ] 无破坏性变更

### 7.3 文档指标

- [ ] 完成 20 章教程
- [ ] 完成 5 篇专题论文
- [ ] 总文档量 > 50,000 字

---

## 八、附录

### A. MiniClaw 最近两周提交统计

| 提交 | 主题 | 类型 |
|:-----|:-----|:-----|
| `7f7071c` | 创世表观遗传学 | 功能 |
| `ab7b4ef` | 端粒守卫 | 功能 |
| `ad285e4` | DNA 路由增强 | 功能 |
| `096e443` | 菌丝共生网络 | 功能 |
| `28457a7` | 终局进化功能 | 功能 |
| `faeb96d` | 文档体系改进 | 文档 |
| `bb598d3` | README 更新 | 文档 |
| `7994df2` | 心跳系统修复 | 修复 |

### B. 参考资源

- [MiniClaw 仓库](https://github.com/8421bit/MiniClaw)
- [MiniClaw 文档](MiniClaw/docs/)
- [MiniClaw 模板](MiniClaw/templates/)
- [myclaw.net 现有对比文档](docs/MiniClaw-vs-myclaw.net-v2.md)

---

**文档版本**: 4.0  
**创建日期**: 2026-03-23  
**负责人**: AI Analysis Team  
**状态**: 待实施
