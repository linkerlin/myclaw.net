# MyClaw.NET 改进方案 v5.1 - 基于当前仓库实况的校正版

> 分析日期: 2026-03-31
> 对照对象: 改进方案和计划.md、TODO.md、当前 src/、当前文档索引
> 核心判断: 现阶段的主要问题不是“缺模块”，而是“已有模块未形成统一主路径”

---

## 📊 执行摘要

### 一句话结论

myclaw.net 目前已经有相当完整的模块储备，但系统真实能力与文档描述之间存在偏差。最需要优先解决的是“统一上下文装配”和“运行时编排”，而不是继续按 MiniClaw 清单补更多孤立特性。

### 当前阶段定位

| 维度 | 当前情况 | 判断 |
|:-----|:---------|:-----|
| DNA 安全写入 | 已落地到 MCP 主路径 | ✅ 可用 |
| 进化 / 甲基化基础 | 已实现并可运行 | ✅ 可用 |
| 工作区感知 | 检测器已实现，但未接入 Agent 主提示词 | ⚠️ 待接线 |
| ACE 上下文编译 | 编译器存在，但主路径未使用 | ⚠️ 待接线 |
| Heartbeat 自主执行 | AI CLI 检测与 fallback 已实现 | ✅ 可用 |
| 无聊 / 菌丝 / 器官退化 | 有模块，但接线不足 | ⚠️ 半成品 |
| 终局进化工具 | `myclaw_dream` 存在，`mutate/reproduce` 仍缺失 | ❌ 部分缺失 |
| 文档体系 | 文档数量不少，但口径不一致、索引已过时 | ⚠️ 需治理 |

### 修订后的判断

1. 当前项目更像“能力仓库”，还不是“统一编排的生命体主内核”。
2. 下一阶段应优先把已有模块接进主流程，而不是继续扩展横向能力。
3. 文档工作的重点不该是立刻追求 20 章教程，而应先建立一份可信的当前事实口径。

---

## 一、基于源码核对的实际现状

### 1.1 已进入主路径的能力

| 能力 | 实际状态 | 说明 |
|:-----|:---------|:-----|
| DNA 安全写入 | 已在线 | `myclaw_update` 已集成 `TelomereGuard` 与 `PurposeMap` |
| 模式检测与进化 | 已在线 | `SignalDetector`、`EvolutionEngine`、`MethylationManager` 已形成基础闭环 |
| 使用统计 | 已在线 | `AnalyticsService` 与 `ToolUsageTracker` 已被 MCP 使用 |
| 每日简报 | 已在线 | `DailyBriefingService` 已暴露为 `myclaw_briefing` |
| Heartbeat 自主执行 | 已在线 | `HeartbeatService`、`AiCliDetector`、`AutonomousExecutor`、`HeartbeatTaskRunner` 已接入 Gateway |
| 统一上下文装配 | 已在线 | `AgentPromptContextBuilder` 已把 workspace / time mode / continuation / affect / nociception / methylation / memory 接入 Agent 主提示词 |
| 运行时编排 | 已在线 | `BoredomEngine`、`MyceliumNetwork` 已接入 Heartbeat / Gateway，`RibosomePruner` 已改用真实统计 |
| 安装 / 发布基础 | 已在线 | `scripts/install.sh`、`scripts/install.ps1`、构建脚本与发布产物目录已存在 |
| 向量记忆 / RAG | 已实现 | 这是相较 MiniClaw 的真实优势，应继续保留 |

### 1.2 已实现但尚未形成产品能力的模块

| 模块 | 当前情况 | 问题性质 |
|:-----|:---------|:---------|
| `myclaw_dream` | 已升级为 dry-run 建议工具 | 仍未直接驱动 DNA 写入，但已不再只是状态报告 |

### 1.3 真正缺失的能力

| 能力 | 状态 | 说明 |
|:-----|:-----|:-----|
| 实体 Apoptosis | 缺失 | `EntityStore` 只有 CRUD 与检索，没有生命周期 |
| `myclaw_mutate` | 缺失 | 只在永生工具名单中出现，未注册为实际工具 |
| `myclaw_reproduce` | 缺失 | 同上 |
| Skill Hooks | 缺失 | `Skill` 元数据尚无 hooks 结构，`SkillManager` 也未触发 hook |
| 平台感知抽象 | 缺失 | 尚无 `PerceptionProvider` 层，macOS 感知无法合理落地 |
| Substrate Harvesting | 缺失 | 尚未实现 |

### 1.4 文档方面的真实问题

当前仓库并不是“只有少量文档”，而是“文档散、旧、重叠”：

- 已有多份计划、进度、总结类文档并行存在。
- `DOCUMENTS-INDEX.md` 仍引用已删除或已过时的文件名。
- `改进方案和计划.md` 与 `TODO.md` 对“已实现 / 待接线 / 缺失”的边界定义不清。
- 文档比代码更容易过时，因此必须先建立单一可信口径。

---

## 二、修订后的总体意见

### 2.1 优先级应该从“补功能”切换为“打通主路径”

现在最缺的不是更多模块，而是把现有模块装进一条稳定可验证的运行链路。尤其是：

- `MyClawAgent` 还在手工拼接 `AGENTS.md`、`SOUL.md`、`HEARTBEAT.md` 和记忆。
- 已有的 ACE、Workspace、TimeMode、Continuation、Affect、Methylation 等能力并没有统一进入 prompt。
- 这直接导致“代码里有能力，产品里看不到能力”。

### 2.2 运行时编排比 MiniClaw 功能对齐更重要

如果继续优先新增 `mutate`、`reproduce`、platform perception 等功能，而不先解决主路径编排，项目只会继续积累“实现了但没用起来”的模块。

### 2.3 文档治理应先于文档扩张

当前不建议把“20 章教程 + 多篇论文”继续列为近期 P0/P1。更现实的做法是：

- 先清理失效索引与过时计划。
- 先把“当前可信现状”固定下来。
- 等主路径稳定后，再围绕真实架构写长期文档。

### 2.4 平台专属能力应后移

当前开发环境是 Windows，项目也已经有跨平台脚本与 .NET 运行时基础。在这种前提下，macOS 专属感知不应继续列为近期高优先级；更合理的顺序是：

1. 先抽象 `PerceptionProvider`
2. 再实现 macOS / Windows 各自 provider
3. 最后把环境感知接入 ACE

---

## 三、修订后的优先级

### 3.1 P0 - 当前必须完成

| 编号 | 任务 | 核心目标 | 预计工作量 |
|:----:|:-----|:---------|:----------:|
| P0-1 | 统一上下文装配主路径 | 让 ACE / Workspace / Continuation / Affect 真正进入 prompt | 2-3 天 |
| P0-2 | 打通现有运行时模块 | 把 Boredom / Mycelium / Ribosome 接进 Heartbeat 或 Gateway | 2-3 天 |
| P0-3 | 修正文档事实口径 | 让计划、TODO、索引不再互相矛盾 | 0.5-1 天 |
| P0-4 | 补齐关键集成测试 | 验证接线后的系统行为，而不是只测单模块 | 1-2 天 |

### 3.2 P1 - 核心能力补完

| 编号 | 任务 | 核心目标 | 预计工作量 |
|:----:|:-----|:---------|:----------:|
| P1-1 | Token 骨架化 | 把 `ContextCompiler` 的粗暴截断升级为结构化压缩 | 1-2 天 |
| P1-2 | 实体 Apoptosis | 给实体增加生命周期与自然衰减 | 1 天 |
| P1-3 | `myclaw_mutate` / `myclaw_reproduce` | 补齐终局进化工具，与模板和永生工具名单对齐 | 1-2 天 |
| P1-4 | 强化 Dream | 从“状态说明”升级为“可行动建议 + 安全 dry-run” | 1 天 |
| P1-5 | Skill Hooks | 为 skill 增加 hook 元数据与触发机制 | 1-2 天 |

### 3.3 P2 - 平台与生态扩展

| 编号 | 任务 | 核心目标 | 预计工作量 |
|:----:|:-----|:---------|:----------:|
| P2-1 | `PerceptionProvider` 抽象 | 为平台感知建立统一接口 | 1 天 |
| P2-2 | macOS / Windows 感知 | 在抽象后分别实现平台能力 | 2-3 天 |
| P2-3 | Substrate Harvesting | 扩展外部技能吸收能力 | 1-2 天 |
| P2-4 | 文档扩张 | 在事实稳定后再写章节教程与专题文档 | 持续任务 |

---

## 四、详细实施计划

### Phase A: 统一上下文装配主路径

**目标**：停止在 `MyClawAgent` 中手工拼接 prompt，改为统一通过上下文装配服务输出。

**建议动作**：

- 引入一个统一的 `AgentContextService` 或 `PromptContextAssembler`。
- 在其中组合：
  - `WorkspaceContextService`
  - `TimeModeManager`
  - `ContinuationDetector`
  - `AffectManager`
  - `MethylationManager`
  - `NociceptionManager`
  - 长期记忆 / 今日记忆
- 最终交给 `ContextCompiler` 做预算与截断控制。

**验收标准**：

- Agent 主提示词中能看到工作区信息、时间模式、会话延续摘要。
- Prompt 不再通过 `BuildSystemPrompt()` 手工拼接多个文件。
- Gateway 与 Heartbeat 使用同一套上下文装配逻辑。
- 至少新增 1 组集成测试覆盖提示词装配。

### Phase B: 打通现有运行时模块

**目标**：把已经有实现的模块真正接到运行时。

**建议动作**：

- 在 `HeartbeatService` 或其上层编排中接入：
  - `BoredomEngine`
  - `MyceliumNetwork.AbsorbSporesAsync`
  - Daily Briefing 触发策略
- 在 DNA 更新或痛觉 / 工具经验写入后触发：
  - `MyceliumNetwork.SecreteSporeAsync`
- 修正 `RibosomePruner` 的输入数据：
  - 使用真实 `bootCount`
  - 使用真实 `toolCalls`
  - 不再用进化次数和空字典代替

**验收标准**：

- 无聊引擎有明确触发入口与冷却策略。
- 菌丝网络至少完成一次分泌 / 吸收闭环。
- 器官退化基于真实统计做决策，不再误删。

### Phase C: 补齐终局进化工具

**目标**：让“永生工具名单”“模板工具定义”“MCP 工具注册”三者一致。

**建议动作**：

- 新增 `myclaw_mutate`
  - 只允许 `SOUL.md` / `IDENTITY.md`
  - 使用 `TelomereGuard`
  - 备份原文件
- 新增 `myclaw_reproduce`
  - 统一打包核心 DNA、skills、entities
  - Windows 下优先用 zip，避免强依赖 tar
- 强化 `myclaw_dream`
  - 结合最近日志、统计、甲基化结果输出建议
  - 第一阶段只做 dry-run，不直接改 DNA

**验收标准**：

- 工具定义、永生工具、MCP 注册保持一致。
- `mutate` / `reproduce` 都有基础测试。
- `dream` 能输出可执行建议，而不是只有状态说明。

### Phase D: 实体与技能演化

**目标**：补上真正缺失而且与“数字生命体”定位强相关的能力。

**建议动作**：

- `EntityStore` 增加 `Vitality`、`LastDecay`、自然衰减与移除逻辑。
- `Skill` frontmatter 增加 hooks 元数据。
- `SkillManager` 增加 hook 调度入口：
  - `onBoot`
  - `onHeartbeat`
  - `onMemoryWrite`
  - `onFileChanged`

**验收标准**：

- 实体 30 天无提及会衰减直至移除。
- Skill hook 至少完成 1 条链路闭环。
- Hook 触发有日志或可观察结果。

### Phase E: 文档治理

**目标**：先建立可信文档，再扩张文档规模。

**建议动作**：

- 将当前计划文档与 TODO 作为近期唯一事实口径。
- 清理 `DOCUMENTS-INDEX.md` 中失效链接。
- 对过时方案文档加上“已归档 / 不再维护”标记。
- 暂不承诺大规模章节教程交付日期。

**验收标准**：

- 新人只看 2-3 份文档就能了解当前状态。
- 索引不再指向已删除文档。
- 已实现 / 待接线 / 缺失 三类状态清晰分层。

---

## 五、当前不建议列为近期 P0 的事项

以下内容并非不做，而是不该继续压过主路径整合：

- 仅针对 macOS 的电池 / DND / Spotlight 感知
- Substrate Harvesting
- 20 章教程与专题论文大规模扩张
- 继续添加新的生物学隐喻模块

---

## 六、成功指标

### 6.1 近期成功标准

- Agent 主提示词改为统一装配并受 token 预算控制
- `WorkspaceContextService`、`TimeModeManager`、`ContinuationDetector` 至少有一处真实调用
- `BoredomEngine`、`MyceliumNetwork`、`RibosomePruner` 已完成运行时接线
- `TODO.md` 与本计划不再互相矛盾

### 6.2 中期成功标准

- `myclaw_mutate` / `myclaw_reproduce` 工具可用
- 实体生命周期机制上线
- Skill hooks 形成最小闭环
- 文档索引完成去腐化

---

## 七、结论

对 myclaw.net 来说，下一阶段最重要的不是“再像 MiniClaw 一点”，而是“让已经写出来的能力真正协同工作”。如果先把统一上下文装配、运行时编排、文档口径三件事做扎实，后续无论是平台感知、终局进化工具，还是大规模文档建设，都会更容易落地，也更不容易继续失真。

---

**文档版本**: 5.1
**创建日期**: 2026-03-31
**修订性质**: 基于当前仓库实况的纠偏版
**状态**: 当前有效
